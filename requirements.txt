# Core ML / RL
torch>=2.2.0
stable-baselines3>=2.3.0
gymnasium>=0.29.0
numpy>=1.26.0

# Configuration
pyyaml>=6.0

# Numerics / path planning
scipy>=1.12.0

# Visualization / logging
matplotlib>=3.8.0

# WPILib NetworkTables bridge (optional – only needed when connecting to a
# live maple-sim / WPILib robot simulation process).
# Install with:  pip install robotpy-ntcore>=2024.3.1
# robotpy-ntcore>=2024.3.1

# Dev / testing
pytest>=8.0.0
pytest-cov>=5.0.0
